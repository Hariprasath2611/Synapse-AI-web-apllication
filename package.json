{
  "name": "synapse-ai-ecosystem",
  "version": "1.0.0",
  "description": "Synapse AI — AI Agent Marketplace and Automation Ecosystem",
  "main": "index.js",
  "scripts": {
    "bootstrap": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:ml": "python -m uvicorn app.main:app --reload --port 8000",
    "dev": "concurrently --kill-others \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ml\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
